{
  "name": "stem-forge-frontend",
  "version": "1.0.0",
  "description": "Front-end for STEM Forge Lesson Plan Generator",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp -r Images *.html *.css *.js dist/ || xcopy /E /I /Y Images dist\\Images && xcopy /Y *.html dist\\ && xcopy /Y *.css dist\\ && xcopy /Y *.js dist\\",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  }
}
